install.packages("proton")
library(proton)
proton()
for (i in 1:1000) {
  proton(action = "login", login="johnins", password=top1000passwords[i])
}

for (i in 1:1000) {
  proton(action = "login", login="johnins", password=top1000passwords[i])
}